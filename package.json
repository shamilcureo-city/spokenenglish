{
  "name": "fluentmap",
  "version": "0.1.0",
  "private": true,
  "description": "FluentMap — a science-driven spoken-English platform for India",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
